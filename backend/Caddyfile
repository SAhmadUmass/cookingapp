localhost {
	# Listen on ports 80 and 443 with automatic HTTPS
	tls internal

	# Reverse proxy to your Node.js backend
	reverse_proxy localhost:8080

	# Enable logging
	log {
		output file ./caddy.log
		format console
	}
}
